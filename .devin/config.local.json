{
  "permissions": {
    "allow": [
      "Exec(npm run)",
      "Exec(ls)",
      "Exec(dir)",
      "Exec(forge init)",
      "Exec(curl)",
      "Exec(mkdir lib)",
      "Exec(mkdir openzeppelin-contracts)",
      "Exec(npm list)"
    ]
  }
}